<div class="container is-flex is-justify-content-center is-flex-wrap-wrap" id="socials">
    <a href="https://steamcommunity.com/id/xwilarg/" target="_blank"><i class="fa-brands fa-steam"></i></a>
    <a href="https://discord.com/users/144851584478740481" target="_blank"><i class="fa-brands fa-discord"></i></a>
    <a href="https://www.youtube.com/@Xwilarg" target="_blank"><i class="fa-brands fa-youtube"></i></a>
    <a href="https://www.twitch.tv/xwilarg" target="_blank"><i class="fa-brands fa-twitch"></i></a>
    <a href="https://github.com/Xwilarg" target="_blank"><i class="fa-brands fa-github"></i></a>
    <a href="https://zirk.itch.io/" target="_blank"><i class="fa-brands fa-itch-io"></i></a>
    <a href="https://ldjam.com/users/xwilarg/games" target="_blank"><i class="fa-solid fa-gamepad"><i class="fa-solid fa-l"></i></i></a>
    <a href="https://globalgamejam.org/users/zirk" target="_blank"><i class="fa-solid fa-gamepad"><i class="fa-solid fa-g"></i></i></a>
    {% if nsfw %}<a href="https://onehourgamejam.com/?page=author&author=xwilarg" target="_blank"><i class="fa-solid fa-gamepad"><i class="fa-solid fa-o"></i></i></a>{% endif %}
</div>
<div class="container is-flex is-flex-wrap-wrap is-justify-content-center">
    {% for elem in home %}
    <div class="card">
        <div class="card-content">
            <div class="content">
                <h2 class="title">{{ elem.name }}</h2>
                <p class="subtitle">{{ elem.catch }}</p>
            </div>
            <div class="content">
                {% if nsfw and elem.link.nsfw or not elem.link.nsfw %}
                    {% if elem.link.target %}
                        <a class="button is-primary target-{{ elem.link.target }}" href="#{{ elem.link.target }}"/>{{ elem.link.name }}</a>
                    {% else %}
                        <a class="button is-primary" href="{{ elem.link.content }}" target="_blank"/>{{ elem.link.name }}</a>
                    {% endif %}
                {% endif %}
            </div>
            <div class="is-flex card-images">
                {% set img = elem.images | filter(x => (nsfw and x.nsfw) or x.nsfw is null or (not nsfw and not x.nsfw)) | first %}
                <div class="is-flex is-align-items-center">
                    <div>
                        <img class="project-image" src="/data/img/{% if img.folder %}{{ img.folder }}{% else %}home{% endif %}/{{ img.name }}"/>
                        <legend>{{ img.description }}</legend>
                    </div>
                </div>
                <div class="menu-container is-flex is-flex-direction-column">
                    {% for key,img in elem.images | filter(x => (nsfw and x.nsfw) or x.nsfw is null or (not nsfw and not x.nsfw))[:3] %}
                        <img class="small {% if key == 0 %}selected{% endif %}" src="/data/img/{% if img.folder %}{{ img.folder }}{% else %}home{% endif %}/{{ img.name }}" alt="{{ project.name }} preview: {{ img.description }}" data-preview="{{ img.description }}"/>
                    {% endfor %}
                </div>
            </div>
        </div>
    </div>
    {% endfor %}
</div>